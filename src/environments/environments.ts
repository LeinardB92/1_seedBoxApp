// Este documento podría ser un .env, combinado con la generación automática de lo que requiera, eso lo hacemos dentro del archivo 'packeage.json'

// El puerto 300 correponde al puerto del bakend en python.
export const environment = {baseUrl: 'http://localhost:3000'}
